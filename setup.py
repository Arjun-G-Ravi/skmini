from setuptools import setup, find_packages

# This lets the repo to be installed using 'pip install -e .' at root
setup(
    name="skmini",
    version="0.1",
    packages=find_packages(),
    install_requires=[
    ],
)
